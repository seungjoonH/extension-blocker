# commit

**REQUIRED:** Read and follow `.cursor/skills/commit/SKILL.md` exactly before doing anything else.

## Context

Gather git context first (in parallel):

- `git status`
- `git diff` (staged and unstaged)
- `git log --oneline -10`

Then stage relevant files and create one commit with a Conventional Commits message in Korean.

Do not push or create a PR unless the user explicitly asks.
