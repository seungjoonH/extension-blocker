# prompt-log

**REQUIRED:** Read and follow `.cursor/skills/prompt-log/SKILL.md` exactly before doing anything else.

Then append a new work log to `docs/ai/PROMPT_LOG.md` per the skill template.

Do not commit unless the user explicitly asks.
