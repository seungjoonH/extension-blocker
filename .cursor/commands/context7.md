# context7

**REQUIRED:** Read `.cursor/skills/context7/SKILL.md` and follow it exactly before any other action.

Use only for library API, configuration, or version-specific behavior via Context7 MCP.

Do NOT use for product policy, requirements decisions, or `결정 필요` items in `docs/specs/`.
