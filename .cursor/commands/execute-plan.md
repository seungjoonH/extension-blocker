# execute-plan

**REQUIRED:** Read the Superpowers `subagent-driven-development` skill and follow it exactly before any other action.

Use when executing `docs/superpowers/plans/*.md` task-by-task. Use Cursor Task tool for per-task implement and review.

If the plan says `executing-plans` instead, read that skill.
