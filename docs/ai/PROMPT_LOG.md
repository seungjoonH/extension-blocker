# Prompt Log

AI 활용 과정과 사용자의 판단을 작업 단위로 기록한다.
기록은 작업 순서에 따라 시간 오름차순으로 배치한다.

## 공통 AI 도구

- **Claude Code**: 프로젝트의 AI 워크플로 구성, 문서 작성, 코드 수정, 검증과 기록을 수행하는 주 Agent로 사용. Skills, MCP, 프로젝트 규칙과 플러그인을 하나의 실행 환경에서 연결하고, 정의한 작업 흐름을 반복 적용하기 위해 선택함
- **Cursor Agent**: Claude Code 5시간 토큰 한도가 잦게 소진되어 검증, 배포 등 후속 작업을 이어가기 위해 도입한 보조 Agent. `.cursor/`에 Superpowers, Context7 MCP, skill, rules, slash commands, hooks를 구성하고 `.claude/skills/`와 동일 내용을 공유해 Claude Code와 같은 워크플로와 skill invoke를 유지함
- **Superpowers**: 기획, 계획 작성, TDD, 체계적 디버깅과 완료 전 검증 절차를 실제 작업 단계에 적용하기 위해 사용. AI가 바로 구현에 들어가지 않고 일정한 개발 절차를 따르도록 구성함
- **Context7**: 라이브러리의 최신 API와 설정을 공식 문서 기준으로 확인하기 위해 사용. AI의 기존 지식이나 버전 차이로 발생할 수 있는 오류와 추측을 줄이기 위해 MCP로 연결함
- **document-review**: Markdown 문서의 문체, 형식, 용어와 프로젝트 상태의 정합성을 일관되게 검토하기 위해 만든 프로젝트 전용 스킬
- **prompt-log**: 핵심 프롬프트, AI 활용 과정과 사용자 판단을 작업 단위로 기록하고 누락을 방지하기 위해 만든 프로젝트 전용 스킬. AI는 사용자의 회고를 대신 작성하지 않고 질문과 판단 대상을 제공하도록 제한함
- **commit-commands / commit skill**: 검증이 끝난 변경사항을 논리적인 작업 단위로 검토하고 커밋하기 위해 사용. Claude Code는 `commit-commands` plugin, Cursor Agent는 `.cursor/skills/commit/` skill로 동일 절차를 수행함

## 작업 기록

## 001. AI 개발 워크플로 구성

### 작업 요약

기능 구현 전에 AI 도구와 스킬의 역할과 실행 순서를 `AI_WORKFLOW.md` 로 먼저 정의하고, 이를 뒷받침하는 프로젝트 스킬을 구성한 뒤 실제 설치와 인식 상태를 검증했다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 최초 요청

```
docs/ai/AI_WORKFLOW.md를 기준으로 현재 AI 개발 환경이 올바르게 구성되었는지 확인하는데, [...] 문제를 직접 수정하거나 스테이징하거나 커밋하지 말아줘.
```

**요청 의도**

> 기능 구현을 시작하기 전에 AI_WORKFLOW.md에 정의한 AI 개발 환경이 실제로 올바르게 구성되어 있는지 확인하고, 수정이 필요한 부분을 먼저 파악하기 위해 요청했습니다.

#### 2. Superpowers, Context7 중복 정리 지시

```
공식 마켓플레이스의 Superpowers 하나만 유지하는데, [...] 별도로 추가한 마켓플레이스 또는 중복 설치본은 제거해줘.
```

**요청 의도**

> AI가 점검한 결과 Superpowers와 Context7이 중복 등록되어 있음을 확인했습니다. 동일한 도구가 여러 경로로 등록되면 실제로 어떤 구성을 사용하는지 불명확해질 수 있다고 생각했습니다. 따라서 공식 설치본만 유지하고 중복 구성을 제거해 AI 개발 환경을 명확하게 정리하기 위해 요청했습니다.

#### 3. prompt-log 회고 대필 금지 지시

```
prompt-log를 사용해서 이번 AI 개발 워크플로 구성 작업의 로그 초안을 생성해줘. [...] 작업 내역과 AI 활용 항목만 채우고, 나의 판단과 회고 항목은 비워둔 채로 남겨줘.
```

**요청 의도**

> AI 활용 과정과 작업 결과는 자동으로 정리하되, 사용자의 판단과 회고는 실제 작업을 진행한 제가 직접 작성해야 한다고 생각했습니다. 따라서 AI가 회고 내용을 대신 작성하지 않고 작성 영역만 남기도록 요청했습니다.

#### 4. 프롬프트 기록 추가 요청

```
prompt-log 스킬의 프롬프트 기록 방식과 현재 docs/ai/PROMPT_LOG.md의 001번 로그를 수정하는데, AI에 입력한 핵심 프롬프트를 기록하되 로그가 지나치게 길어지지 않도록 가독성과 선별 기준을 강화해줘.
```

**요청 의도**

> 과제에서 AI에 입력한 프롬프트와 재질문 과정을 기록하도록 요구하고 있어, 작업의 흐름에 영향을 준 핵심 프롬프트를 남길 필요가 있다고 생각했습니다. 다만 모든 대화를 기록하면 로그가 지나치게 길어질 수 있어, 핵심 프롬프트만 선별하고 접이식으로 구성해 가독성을 유지하기 위해 요청했습니다.

</details>

### AI 활용 과정

- Superpowers, Context7, commit-commands의 실제 설치와 인식 상태를 점검함
- `AI_WORKFLOW.md`, `CLAUDE.md`, 각 `SKILL.md` 간 설명 불일치를 점검해 제시함

### 최종 반영

- `docs/ai/AI_WORKFLOW.md` 작성 및 `document-review`, `prompt-log` 프로젝트 스킬 구성
- `prompt-log`가 사용자 판단을 대신 작성하지 않도록 정책 확정
- Superpowers, Context7, commit-commands 중복 등록과 설치 오류 정리
- 문서 간 설명 불일치 수정 및 `.gitignore`에 `.claude/settings.local.json` 추가

### 나의 판단과 회고

#### AI의 제안 중 그대로 반영한 내용은 무엇인가?

이번 작업에서는 다음 제안이 실제 반영 후보에 포함되었습니다.

- 기능 구현 전에 `AI_WORKFLOW.md`를 먼저 작성하는 방식
- `document-review`와 `prompt-log`를 프로젝트 전용 스킬로 분리하는 방식

> 기능 구현 전에 AI 활용 방식과 작업 절차를 먼저 정의하는 방향을 그대로 채택했습니다. 구현 후 활용 과정을 복기하면 실제 판단이나 수정 과정이 누락될 수 있다고 생각했습니다. 또한 문서 검토와 AI 활용 기록의 역할이 다르므로 `document-review`와 `prompt-log`를 각각 분리해 구성했습니다.

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

이번 작업에서는 다음 항목을 여러 차례 수정했습니다.

- `prompt-log`의 로그 구조와 분량
- 고정된 회고 항목을 작업 맥락에 맞는 질문으로 변경
- 유도성 있는 회고 질문을 중립적인 질문으로 변경
- 모든 프롬프트가 아닌 핵심 프롬프트만 기록하도록 범위 제한

> 초기 `prompt-log`에는 실제 입력 프롬프트가 포함되지 않았고, 회고 질문에 사용자의 판단 근거가 미리 들어가 있었습니다. 이를 직접 확인한 뒤 핵심 프롬프트와 요청 의도를 기록하도록 보완하고, 작업 맥락에 맞는 중립적인 회고 질문을 제공하도록 수정했습니다. 또한 로그가 길어지지 않도록 핵심 프롬프트만 접이식으로 기록하도록 변경했습니다.

#### AI의 제안 중 제외한 내용은 무엇인가?

이번 작업에서는 다음 제안이나 작성 방식의 제외 여부를 판단했습니다.

- AI가 사용자의 판단과 회고 답변까지 대신 작성하는 방식
- 작업 결과에 영향을 주지 않은 모든 대화를 프롬프트 로그에 기록하는 방식
- 도구 선택 이유와 AI 결과 판단을 하나의 회고 영역에 함께 작성하는 방식

> AI가 사용자의 판단과 회고까지 작성하는 방식은 제외했습니다. 회고에는 실제 사용자의 선택과 근거가 들어가야 한다고 생각했기 때문입니다. 또한 모든 대화를 기록하면 핵심 판단 과정이 묻힐 수 있어 제외했으며, 도구 선택 이유는 공통 AI 도구 영역으로 분리하고 회고는 AI 결과를 채택, 수정, 제외한 판단에만 집중하도록 정리했습니다.

---

## 002. 파일 업로드 요구사항 문서화

### 작업 요약

과제 원문을 기준으로 `docs/specs/REQUIREMENTS.md`를 작성하고, 필수 요구사항과 구현 범위와 의사결정, 추가 고려사항, 검증 체크리스트 네 단계로 문서를 구성했다. 결정 필요로 남은 24개 항목을 사용자와 하나씩 논의해 확정하고, document-review로 문서 품질을 검증해 발견된 문제를 수정했다. 이후 유일하게 남아 있던 미확정 항목인 업로드 파일명의 구체적인 최대 길이를 논의해 확정하고 검증 체크리스트를 닫았다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 최초 요청

```
과제 원문을 기준으로 요구사항을 분석해서 docs/specs/REQUIREMENTS.md에 작성해줘. 문서는 다음 순서로 구성하는데, [...] 아직 판단하지 않은 항목은 임의로 결정하지 말고 결정 필요로 표시해줘.
```

**요청 의도**

> 과제 원문의 필수 요구사항과 추가 고려사항을 빠짐없이 구조화하되, 아직 논의하지 않은 내용을 AI가 임의로 확정하지 않도록 요청했습니다. 구현 전에 요구사항과 결정이 필요한 쟁점을 분리해 이후 논의의 기준이 되는 문서를 만들고자 했습니다.

#### 2. 논의 진행 방식 지시

```
docs/specs/REQUIREMENTS.md의 결정 필요로 남아 있는 항목을 사용자와 하나씩 논의해 구체화해줘. [...] 한 번에 여러 항목을 결정하지 말아줘.
```

**요청 의도**

> 여러 쟁점을 한꺼번에 결정하면 각 선택의 근거와 영향이 섞일 수 있으므로, 항목별 의미와 선택지를 확인한 뒤 하나씩 결정하고자 했습니다. 논의 과정에서는 세부 항목별로 기존 요구사항을 구체화하거나 수정하고, 과제의 본질과 관련이 없거나 다른 항목과 중복되는 내용은 삭제했습니다. 각 과정에서 이루어진 사용자의 구체적인 판단과 변경 근거는 나의 판단과 회고에 통합해 정리했습니다.

#### 3. 품질 검토 요청

```
기존 AI workflow에 따라 현재 요구사항 문서의 품질을 검토해줘. 다음 항목만 확인해줘. [...] 문제가 발견되면 임의로 수정하지 말고, 문제 위치와 이유, 수정 방향만 정리해줘.
```

**요청 의도**

> 모든 논의가 끝난 뒤 문서의 결정 내용이 서로 일관되는지, 질문과 답변이 같은 쟁점을 다루는지, 중복이나 삭제 흔적이 남아 있지 않은지를 커밋 전에 확인하고자 했습니다. AI가 확정된 내용을 임의로 변경하지 않도록 문제점과 수정 방향만 먼저 보고하도록 요청했습니다.

#### 4. 파일명 길이 확정 요청

```
파일명 길이 제한은 데이터베이스나 프레임워크에 따라 정하는 구현 방식이 아니라 사용자가 업로드할 수 있는 입력 범위를 정하는 정책이고, 기술 설계로 넘기면 요구사항 문서와 체크리스트가 미완성 상태로 남으니까, 요구사항 단계에서 255자로 확정해줘. [...]
```

**요청 의도**

> 파일명 길이 제한은 구현 방식이 아니라 사용자 입력 범위를 정하는 정책이므로 기술 설계 단계로 미루지 않고 요구사항 단계에서 확정해야, 요구사항 문서와 검증 체크리스트가 완결된 상태로 남는다고 판단해 요청했습니다.

</details>

### AI 활용 과정

- 4단계 구조에 맞춰 문서를 작성하면서, 과제 원문 대비 누락된 세부 요구사항이 있는지 점검함
- 결정 필요 24개 항목마다 의미, 선택지, 장단점과 권장안을 제시하고, 다른 항목과 겹치는 경우(정책 변경 로그, 연속된 마침표, 동일 파일 재업로드)는 새로 논의하지 않고 기존 항목을 참조하도록 안내함
- document-review로 7개 기준(일관성, 질문과 결정의 일치, 중복, 카테고리 배치, 재논의 여부, 삭제 흔적, 체크리스트 반영)을 검토해 체크리스트 누락 1건과 용어 불일치 1건을 발견해 보고함
- 품질 검토 이후 유일하게 남아 있던 미확정 항목(업로드 파일명 길이 제한의 구체적인 수치)을 확인해 보고함

### 최종 반영

- `docs/specs/REQUIREMENTS.md`를 필수 요구사항, 구현 범위와 의사결정, 추가 고려사항, 검증 체크리스트 4단계 구조로 작성
- 검증과 보안, 정책과 데이터, UX와 예외, 운영과 확장 하위 24개 항목을 사용자와 개별 논의해 결정하고 적용 방향, 판단 이유, 예시를 반영
- 결정하지 않기로 한 3개 항목(사용자별 정책 확장, 화이트리스트 전환, 동일 파일 재업로드)을 문서에서 삭제
- document-review로 발견한 검증 체크리스트 누락(업로드 파일명 길이 제한 항목)과 용어 불일치(`Storage`를 `저장소`, `저장 식별자`로 통일)를 수정
- `업로드 파일명 길이 제한` 항목에 최대 255자 제한과 판단 이유 반영, 검증 체크리스트를 `업로드 파일의 원본 파일명이 확장자를 포함해 255자를 초과하면 서버에서 거부됩니다`로 확정

### 나의 판단과 회고

#### AI의 제안 중 그대로 반영한 내용은 무엇인가?

이번 작업에서는 다음 논의에서 AI가 제시한 권장안을 그대로 선택했습니다.

- 서버 사이드 검증, 대소문자 정규화, 확장자 없는 파일 처리의 기본 방향
- 빈 입력 처리, 연속된 마침표 처리, 반응형 레이아웃 방식

> 클라이언트 검증만으로는 요청 변조나 직접적인 API 호출을 막을 수 없으므로, 서버가 현재 정책을 기준으로 최종 검증해야 한다는 제안을 그대로 반영했습니다. 또한 대소문자 차이와 선행 마침표처럼 의미가 동일한 입력은 정규화하고, 빈 입력이나 연속된 마침표처럼 의미가 불분명한 값은 명확하게 거부하는 방식이 일관적이라고 판단했습니다. 반응형은 복잡한 브레이크포인트를 구성하기보다 단순한 화면 구조에 맞춰 요소가 자연스럽게 줄바꿈되도록 하는 방향을 선택했습니다.

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

이번 작업에서는 다음 논의에서 AI가 제시한 선택지를 조합하거나 세부 조건을 바꿔 반영했습니다.

- 확장자 없는 파일과 숨김 파일 처리(숨김 파일 예외를 없애고 접미사 규칙으로 통일)
- 저장 실패 처리(고정 확장자는 낙관적 업데이트, 커스텀 확장자는 서버 응답 후 반영으로 유형별 분리)
- 파일 시그니처 검증(매직 넘버 직접 관리 대신 ClamAV 기반 보안 필터로 재구성)
- 고정 확장자와 커스텀 확장자 중복 처리(등록 거부 대신 자동 활성화 방식 선택)

> 숨김 파일을 항상 확장자가 없는 파일로 처리하면 env 정책으로 .env를 차단하려는 사용자의 기대와 어긋날 수 있다고 생각해, 일반 파일과 동일한 접미사 규칙을 적용하기로 했습니다. 저장 방식은 모든 변경에 같은 전략을 적용하기보다, 연속 조작 가능성이 있는 고정 확장자와 그렇지 않은 커스텀 확장자를 다르게 처리하는 것이 안전하다고 판단했습니다. 파일 보안 검사는 애플리케이션이 매직 넘버 목록을 직접 관리하는 것보다 확장자 정책과 ClamAV 검사를 독립된 필터로 구성하는 것이 역할과 책임을 명확히 할 수 있다고 판단했습니다. 커스텀 입력란에 고정 확장자를 입력한 경우에도 단순히 오류로 거부하기보다 해당 고정 확장자를 활성화하는 것이 사용자의 차단 의도를 더 직접적으로 반영할 수 있다고 판단했습니다.

#### AI의 제안 중 제외한 내용은 무엇인가?

이번 작업에서는 다음 항목 자체를 논의 없이 제외했습니다.

- 사용자별 정책 확장, 화이트리스트 전환, 동일 파일 재업로드

> 사용자별 정책과 화이트리스트 전환은 현재 과제에서 요구하지 않는 미래 확장에 대한 논의이므로 문서의 핵심을 흐릴 수 있어 삭제했습니다. 동일 파일 재업로드는 원본 파일명 저장 항목에서 독립 저장과 덮어쓰기 금지 원칙이 이미 확정되어 있어 별도 논의로 유지할 필요가 없다고 판단했습니다.

---

## 003. 기획 문서 작성

### 작업 요약

`docs/specs/REQUIREMENTS.md`에서 확정된 요구사항을 화면 구성, 사용자 흐름, 상태와 상호작용으로 구체화해 `docs/specs/PLANNING.md`를 새로 작성했다. 화면 구조, 알림 패턴, 업로드 상호작용, 커스텀 확장자 삭제 방식, 문서 저장 경로 등 기획 단계의 주요 쟁점을 사용자와 하나씩 논의해 결정하고, 문서 작성 후 문서 간 정합성과 기획 공백을 여러 차례 검토해 발견된 모순과 누락을 보완했다. 문서 승인 후 `docs/WORKFLOW.md`의 기획 단계 상태를 완료로 갱신했다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 최초 요청

```
이제 Workflow의 3단계인 기획을 진행하는데, 확정된 요구사항을 사용자 흐름, 화면 구성, 상태와 상호작용으로 구체화해줘. [...] 곧바로 기획 문서를 작성하지 말고, 먼저 관련 문서를 분석해서 기획을 위해 결정해야 할 항목까지 정리해서 보고해줘.
```

**요청 의도**

> 확정된 요구사항을 바로 문서화하기보다, 먼저 사용자 흐름과 화면 상태로 구체화하기 위해 필요한 결정 사항을 빠짐없이 확인하고자 요청했습니다. 요구사항 분석과 기획 문서 작성을 분리해, 사용자의 결정 없이 AI가 화면 구조나 상호작용을 임의로 확정하지 않도록 하는 것이 목적이었습니다.

#### 2. 알림 패턴 결정

```
저장 성공과 실패, 자동 활성화 안내, 네트워크 오류, 업로드 차단 사유 같은 피드백을 인라인 고정 영역, 토스트, 혼합 방식 중에서 고르는데, 혼합 방식으로 하되 중요도만 추상적으로 구분하지 말고 메시지 성격에 따라 표시 위치를 명확히 고정해줘. 저장 성공과 자동 활성화 안내는 토스트로, 저장 실패와 네트워크 오류는 닫기 가능한 토스트로, 입력값 검증 오류는 입력 필드 아래 인라인으로, 업로드 차단 사유와 업로드 결과는 업로드 영역 안에 인라인으로 표시해줘. [...]
```

**결정 이유**

> 모든 메시지를 화면에 고정하면 복잡해지고, 모두 토스트로 처리하면 사용자가 수정하거나 확인해야 하는 정보가 사라질 수 있다고 생각했습니다. 따라서 일시적인 작업 결과는 토스트로 제공하고, 입력 오류와 업로드 차단 사유는 관련 영역에 유지하는 방식이 적절하다고 판단했습니다.

#### 3. 업로드 결과 표시 방식 결정

```
파일 업로드 결과 표시 방식을 정하는데, 가장 최근 업로드 시도의 결과 1건만 남겨줘. 여기서 결과는 업로드된 파일 목록이 아니라 방금 시도한 업로드가 성공했는지, 차단됐는지, 실패했는지와 그 사유를 보여주는 피드백을 뜻해. [...] 이전 업로드 결과는 화면에 누적하지 말고, 업로드 이력 목록은 이번 과제 범위에 포함하지 말아줘.
```

**결정 이유**

> 이 화면은 업로드 이력을 조회하는 화면이 아니라, 가장 최근 업로드 시도에 현재 정책이 어떻게 적용됐는지 확인하는 화면이라고 생각했습니다. 결과를 계속 누적하면 업로드 목록처럼 보이고 화면도 복잡해질 수 있으므로, 가장 최근 업로드 시도의 결과 1건만 표시하는 것이 적절하다고 판단했습니다.

#### 4. 문서 간 정합성과 기획 공백 검토 요청

```
문서 간 정합성과 기획 공백이 있는지 다시 확인하는데, 없을 때까지 반복할 거니까 한 번에 최대한 많이 찾아줘.
```

**요청 의도**

> 요구사항과 기획 문서 사이의 모순이나 누락이 구현 단계까지 이어지는 것을 방지하기 위해 요청했습니다. 동일한 검토를 여러 차례 반복하기보다, 문서 전체를 한 번에 대조해 이미 결정된 내용의 반영 누락과 추가 결정이 필요한 기획 공백을 최대한 많이 발견하고자 했습니다.

#### 5. 서버 검증 순서 확정

```
서버 검증 순서를 정하는데, 파일명 기본 검증, 확장자 차단 정책 검증, 파일 크기 검증, ClamAV 검사, 저장 순서로 확정해줘. [...] 하나의 파일이 여러 조건에 걸리더라도 가장 먼저 실패한 사유 하나만 표시해줘.
```

**결정 이유**

> 처리 비용이 낮고 즉시 판별 가능한 검증부터 수행하는 것이 효율적이라고 판단했습니다. 특히 확장자 차단은 이번 과제의 핵심 정책이므로 파일명 기본 검증 다음에 적용하고, 상대적으로 비용이 큰 ClamAV 검사는 파일명, 확장자, 크기 검증을 모두 통과한 파일에만 수행하기로 결정했습니다.

#### 6. 저장 대기 중 이탈 방지 방식 결정

```
고정 확장자 저장 대기 중 페이지 이탈 방지 방법을 정하는데, `beforeunload`로 브라우저 기본 확인 대화상자를 표시하는 방식으로 결정해줘. [...] 이탈 시 저장 요청을 조용히 전송하는 방식에만 의존하지 말아줘. [...]
```

**결정 이유**

> Debounce 저장 대기 중 페이지를 이탈하면 사용자가 저장되지 않은 변경사항을 인지하지 못한 채 잃을 수 있다고 생각했습니다. 이탈 시 저장을 조용히 시도하는 방식은 네트워크 오류가 발생해도 실패 여부를 확인하기 어려우므로, 미저장 상태에서는 브라우저 기본 확인 대화상자를 표시하는 것이 더 안전하다고 판단했습니다.

</details>

### AI 활용 과정

- brainstorming 방식에 따라 `REQUIREMENTS.md`와 `WORKFLOW.md`를 먼저 분석해 핵심 사용자, 화면 후보, 사용자 흐름과 결정이 필요한 쟁점을 먼저 보고하고, 문서 작성 전 사용자 승인을 받음
- 화면 구조, 알림 패턴, 업로드 상호작용(파일 선택 방식, 진행 상태 표시, 결과 누적 여부), 커스텀 확장자 삭제 확인 여부, 문서 저장 경로를 각각 A/B/C 선택지와 장단점, 권장안으로 제시해 사용자가 하나씩 결정하도록 진행
- 문서 작성 후 AI가 자체적으로 여러 차례 `REQUIREMENTS.md`와 `PLANNING.md`를 대조해 문서 간 정합성과 기획 공백을 검토했고, 발견한 모순과 누락 중 이미 결정된 내용을 반영하는 것은 사용자 승인 후 바로 수정하고 새로운 정책 결정이 필요한 것은 선택지를 제시해 사용자 결정을 받음
- Markdown이 변경될 때마다 `document-review` 스킬을 실행해 `~한다`체 통일, 가운뎃점 미사용, 표와 Mermaid 문법, `docs/WORKFLOW.md` 상태 일치 여부를 검증

### 최종 반영

- `docs/specs/PLANNING.md` 신규 작성(사용자 흐름과 화면 구성, 상태별 표시와 오류 노출 방식, 접근성과 반응형 차이, 요구사항 추적 관계, 기술 설계로 넘길 결정 포함)
- `docs/WORKFLOW.md`의 3단계(기획) 산출물에 `docs/specs/PLANNING.md` 링크 추가, 현재 상태를 완료로 갱신

### 나의 판단과 회고

#### AI의 제안 중 그대로 반영한 내용은 무엇인가?

이번 작업에서는 다음 논의에서 AI가 제시한 권장안을 그대로 선택했습니다.

- 단일 화면, 섹션 구분 화면 구조
- 파일 선택은 클릭 선택만 지원(드래그앤드롭 제외)
- 업로드 진행 상태는 불확정 스피너와 텍스트로 표시
- 커스텀 확장자 삭제는 확인 절차 없이 즉시 처리
- 기획 문서 저장 경로는 `docs/specs/PLANNING.md`

> 정책 관리와 파일 업로드는 서로 다른 역할을 가지지만, 정책을 변경한 직후 실제 업로드 결과를 확인하는 흐름이 핵심이라고 생각했습니다. 따라서 별도 화면으로 나누기보다 한 페이지 안에서 영역만 명확히 구분하는 방식을 선택했습니다. 파일 업로드는 필수 기능에 집중하기 위해 클릭 선택만 지원하고, 진행률을 정확히 계산할 필요가 없으므로 스피너와 상태 텍스트로 처리했습니다. 커스텀 확장자 삭제는 복구하기 어려운 데이터가 아니고 다시 등록할 수 있으므로 별도의 확인 절차 없이 즉시 처리하는 것이 적절하다고 판단했습니다.

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

이번 작업에서는 다음 논의에서 AI가 제시한 선택지를 조합하거나 세부 조건을 바꿔 반영했습니다.

- 알림 패턴(토스트와 인라인 구분을 AI의 일반적인 권장안보다 메시지 성격별로 세밀하게 직접 지정)
- 업로드 결과 표시(누적 이력 대신 직전 시도 1건만 표시, 동일 파일 재시도도 새 시도로 처리)
- 서버 검증 순서(파일명, 확장자, 크기, ClamAV, 저장 순서를 직접 확정)
- 여러 차례의 정합성 검토에서 발견된 모순과 공백(빈 상태와 업로드 성공 상태의 충돌, 파일명 검증에서 빈 파일명과 길이 초과의 클라이언트/서버 구분, 파일 하나만 선택 가능하다는 명시 누락 등)을 보완
- 저장 대기 중 페이지 이탈 방지 방식(`beforeunload` 확인 대화상자로 확정)

> 알림은 메시지 성격에 따라 토스트와 인라인으로 구분했습니다. 저장 결과처럼 일시적인 피드백은 토스트로 제공하고, 입력 오류와 업로드 차단 사유처럼 사용자가 확인하거나 수정해야 하는 정보는 관련 영역에 유지했습니다. 업로드 결과는 이력처럼 누적하지 않고 최근 시도 1건만 표시했으며, 동일 파일 재시도도 새로운 업로드 시도로 처리했습니다. 서버 검증은 파일명, 확장자, 크기, ClamAV 순으로 구성하고, 문서 검토에서 발견된 상태 충돌과 누락을 요구사항 기준으로 보완했습니다. 저장 대기 중 이탈은 변경사항 유실을 막기 위해 브라우저 기본 확인 대화상자를 사용하기로 결정했습니다.

#### AI의 제안 중 제외한 내용은 무엇인가?

이번 작업에서는 다음 선택지를 검토했지만 채택하지 않았습니다.

- 정책 관리와 업로드를 별도 화면으로 분리하는 안
- 드래그앤드롭 업로드 지원
- 모든 메시지를 토스트로 통일하는 안
- 저장 대기 중 이탈 시 조용히 저장을 시도(flush)하는 방식

> 정책 관리와 업로드를 별도 화면으로 분리하면 화면 책임은 명확해지지만, 정책 변경 후 적용 결과를 확인하기 위해 이동해야 하므로 핵심 사용자 흐름이 끊긴다고 판단했습니다. 드래그앤드롭은 사용성을 높일 수 있지만 클릭 선택만으로 필수 기능을 충분히 제공할 수 있어 범위에서 제외했습니다. 모든 메시지를 토스트로 처리하면 입력 오류나 업로드 차단 사유가 자동으로 사라질 수 있으므로 채택하지 않았습니다. 페이지 이탈 시 저장을 자동으로 시도하는 방식도 네트워크 오류가 발생하면 사용자가 유실 사실을 확인하기 어려워 제외했습니다.

---

## 004. 기술 설계 문서 작성과 구현 계획 수립

### 작업 요약

`docs/specs/REQUIREMENTS.md`와 `docs/specs/PLANNING.md`를 바탕으로 기술 설계를 진행했다. 데이터 모델, API, 파일 업로드 처리 흐름, 오류 처리와 테스트 방향을 정리해 `docs/specs/DESIGN.md`를 작성했다.

설계 과정에서 발견한 요구사항과 기획 문서의 누락 및 충돌을 사용자 확인 후 보완했다. 기술 설계가 확정된 뒤에는 writing-plans를 사용해 `docs/superpowers/plans/2026-07-31-file-upload-extension-policy.md`에 26개 태스크로 구성된 구현 계획을 작성했다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 최초 요청

```
docs/WORKFLOW.md를 보고 이제 기술 설계를 들어가야 하는데, superpowers 기반으로 어떻게 진행해야 하는지 추천해줘.
```

**요청 의도**

> 기술 설계 단계에서 검토해야 할 항목과 진행 순서를 먼저 확인하고, 주요 결정을 하나씩 검토하기 위해 요청했습니다.

#### 2. 기술 설계 보완 요청

```
작성한 기술 설계를 다시 검토해서 데이터 모델의 제약조건, API 오류 응답, 파일 업로드 검증 순서, 저장 실패 시 처리 방식이 기존 요구사항과 일치하도록 보완해줘. 이미 확정된 REQUIREMENTS.md와 PLANNING.md의 내용을 변경하지 말고, 문서 사이에 충돌하거나 누락된 부분이 있으면 함께 찾아서 수정해줘.
```

**재요청 이유**

> 기술 설계 초안에 기존 요구사항이 일부 누락되거나 문서 사이의 표현이 다르게 작성된 부분이 있어, 구현 전에 전체 흐름과 기준을 일관되게 맞추기 위해 다시 요청했습니다.

#### 3. 문서 전체 가독성 개선 요청

```
DESIGN.md 전체를 다시 검토해서 기술적 의미를 바꾸지 않는 범위에서 장황한 문장을 정리해줘. 한 문장에 판단, 이유, 예외가 함께 들어가 있으면 나누고, 코드와 표에서 이미 설명한 내용은 본문에서 반복하지 말아줘. 문서 전체의 ~한다체를 유지하고, 가운뎃점은 사용하지 말아줘. 수정 후 REQUIREMENTS.md, PLANNING.md, DESIGN.md 사이의 정합성도 다시 확인해줘.
```

**재요청 이유**

> 기술 설계 내용은 확정됐지만 긴 문장과 반복 설명이 많아 핵심 내용을 빠르게 파악하기 어려웠습니다. 구현 과정에서 참고하기 쉬운 문서로 만들기 위해 기술적 의미는 유지하면서 문장과 구성을 정리하도록 요청했습니다.

</details>

### AI 활용 과정

- brainstorming을 사용해 배포 구조, 데이터 모델, API, 파일 업로드 처리 방식 등 기술 설계에 필요한 주요 항목을 하나씩 검토함
- AI가 제시한 선택지와 장단점을 비교한 뒤 사용자 확인을 거쳐 설계 방향을 확정함
- 기술 설계 초안을 `REQUIREMENTS.md`와 `PLANNING.md`에 반복 대조해 누락되거나 충돌하는 내용을 보완함
- 관련 라이브러리 사용 방식이 불명확한 부분은 Context7을 통해 공식 문서를 확인하고 설계에 반영함
- 문서가 변경될 때마다 `document-review`를 실행해 문체, 형식, 절 번호 참조와 문서 간 정합성을 검토함
- 기술 설계 확정 후 writing-plans를 사용해 구현 작업을 26개 태스크로 나누고, 각 태스크를 테스트와 구현 단위로 정리함

### 최종 반영

- `docs/specs/DESIGN.md` 신규 작성
  - 아키텍처
  - 데이터 모델
  - API 설계
  - 파일 업로드 처리 흐름
  - 오류 처리
  - 테스트 전략
- `docs/specs/REQUIREMENTS.md`에 기술 설계 과정에서 확정된 오류 유형과 파일명 길이 기준 반영
- `docs/specs/PLANNING.md`에 파일명 오류와 요청 크기 초과 상태 반영
- `docs/WORKFLOW.md`의 기술 설계 단계 상태와 산출물 링크 갱신
- `docs/superpowers/plans/2026-07-31-file-upload-extension-policy.md`에 26개 태스크로 구성된 구현 계획 작성

### 나의 판단과 회고

#### AI의 제안 중 그대로 반영한 내용은 무엇인가?

이번 작업에서는 다음 제안을 그대로 선택했습니다.

- Next.js와 ClamAV를 하나의 서버 환경에서 함께 운영하는 방식
- 고정 확장자와 커스텀 확장자를 하나의 테이블에서 관리하는 방식

> 서비스 규모와 과제 범위를 고려했을 때 애플리케이션과 ClamAV를 하나의 서버 환경에서 함께 운영하는 방식이 적절하다고 판단했습니다. 또한 고정 확장자와 커스텀 확장자는 모두 파일 업로드를 제한하기 위한 정책이므로, 하나의 테이블에서 관리하는 방식이 구조를 단순하게 유지하는 데 적절하다고 생각했습니다.

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

이번 작업에서는 기술 설계 초안을 검토하면서 요구사항과 맞지 않거나 설명이 부족한 부분을 수정했습니다.

- 요구사항과 기획 문서에 맞게 기술 설계의 누락과 충돌을 보완
- 문서 간 용어, 상태와 오류 처리 기준을 일관되게 정리
- `DESIGN.md`의 긴 문장과 반복 설명을 정리

> 초기 설계안을 그대로 확정하지 않고, 기존 요구사항과 기획 문서를 기준으로 누락되거나 충돌하는 부분을 반복해서 검토했습니다. 또한 구현 단계에서 문서를 빠르게 이해할 수 있도록 긴 문장과 중복 설명을 정리하고, 문서 전체의 표현과 기준을 일관되게 맞췄습니다.

---

## 005. 구현 실행(Subagent-Driven Development, Task 1~26)과 최종 브랜치 리뷰

### 작업 요약

`docs/superpowers/plans/2026-07-31-file-upload-extension-policy.md`의 26개 태스크를 subagent-driven-development로 실행했다. 태스크마다 구현과 리뷰를 분리한 서브에이전트를 디스패치하고, 발견된 문제는 수정 후 재검토했다. 서버(Supabase 마이그레이션, RPC, 정책/업로드 API, ClamAV 연동, 구조화 로거)와 프런트엔드(5개 화면 섹션, 전역 토스트, 단일 화면 컴포지션)를 모두 구현한 뒤, 전체 브랜치를 다시 리뷰해 기능 간 연결과 보안, 오류 처리를 점검하고 발견된 문제를 수정했다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 토스트 요구사항 보완

```text
Task 21~26의 계획을 PLANNING.md와 다시 비교해서 토스트 규칙이 실제 화면에 연결되도록 보완해줘.

성공 알림은 자동으로 사라지게 하고, 실패 알림은 사용자가 닫을 수 있게 해줘. [...]

입력 오류와 업로드 결과는 기존대로 인라인으로 유지하고, 계획 수정 후 구현을 진행해줘.
```

**재요청 이유**

> 토스트 기능을 만드는 계획은 있었지만 실제 화면과 연결하는 내용이 빠져 있어, 기획 문서에 맞게 계획부터 보완하도록 요청했습니다.

#### 2. 업로드 타임아웃 확정

```text
업로드 요청이 60초 동안 완료되지 않으면 중단하도록 구현해줘.

파일 선택은 유지하고 버튼을 다시 활성화하며, 자동 재시도 없이 사용자가 직접 다시 시도할 수 있게 해줘. [...]

60초 기준은 DESIGN.md에도 기록해줘.
```

**요청 의도**

> 업로드 타임아웃을 구현하기로 했지만 구체적인 시간이 정해져 있지 않아, AI가 임의로 결정하지 않도록 시간과 실패 후 동작을 직접 확정했습니다.

</details>

### AI 활용 과정

- `superpowers:subagent-driven-development`를 사용해 26개 태스크를 순서대로 구현함
- 각 태스크마다 구현 서브에이전트와 리뷰 서브에이전트를 분리해 요구사항 충족 여부와 코드 품질을 검토함
- 중요한 문제가 발견되면 수정 후 다시 검토하고, 가벼운 문제는 `progress.md`에 기록함
- 구현 계획과 `PLANNING.md`를 비교해 빠진 요구사항을 구현 전에 보완함
- 26개 태스크 완료 후 전체 브랜치를 다시 리뷰해 기능 간 연결, 보안과 오류 처리를 점검함

### 최종 반영

- Supabase 기반 데이터베이스, 정책 설정과 파일 저장 구조 구성
- 고정 확장자, 커스텀 확장자와 업로드 크기 설정 API 구현
- 파일명, 확장자, 크기와 악성 파일 검사 흐름 구현
- 검사 완료 파일의 Storage 저장과 실패 시 정리 처리 구현
- 정책 설정, 파일 업로드와 알림 기능을 하나의 화면으로 연결
- 키보드 포커스 표시, 줄바꿈과 가로 스크롤 방지 등 접근성 및 반응형 보완
- 업로드 요청이 60초 이상 지연될 때 중단하도록 타임아웃 적용
- 전체 리뷰에서 발견된 문제(ClamAV 판정 불명확 처리, 로깅 누락, 정책 데이터 재동기화 누락 등) 수정

### 나의 판단과 회고

#### AI의 제안 중 그대로 반영한 내용은 무엇인가?

이번 작업에서는 다음 제안을 그대로 반영했습니다.

- 서브에이전트 기반 태스크 분할과 태스크별 리뷰
- 전체 구현 완료 후 브랜치 전체 재검토

> 전체 기능을 한 번에 구현하면 문제가 발생했을 때 원인을 찾기 어렵다고 생각했습니다. 작업을 작은 단위로 나누고 각 태스크마다 리뷰를 거치면 문제를 일찍 발견할 수 있고, 개별 태스크에서는 보이지 않는 기능 간 연결 문제는 전체 구현이 끝난 뒤 다시 검토해야 발견할 수 있다고 판단했습니다.

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

이번 작업에서는 AI가 작성한 구현 계획을 그대로 실행하지 않고, 기존 요구사항과 비교해 누락된 내용을 먼저 보완했습니다.

- 토스트가 실제 화면과 연결되도록 구현 계획 수정
- 입력 오류와 업로드 결과의 표시 위치를 기획 문서에 맞게 유지
- 업로드 요청 타임아웃을 60초로 직접 확정
- 접근성과 반응형 항목을 이번 구현 범위에 포함

> 구현 계획에 일부 사용자 경험 관련 요구사항이 빠져 있어 바로 구현하지 않고 계획부터 수정했습니다. 특히 알림 기능이 만들어지기만 하고 실제 화면에 연결되지 않는 문제를 확인해, 기획 문서의 기준에 맞게 보완한 뒤 구현을 진행했습니다. 또한 업로드 타임아웃처럼 문서에 구체적인 값이 없는 항목은 AI가 임의로 정하지 않도록 직접 기준을 확정했습니다. 접근성과 반응형 화면도 구현해야 한다고 판단해 다음 단계로 미루지 않고 이번 구현에 포함했습니다.

---

## 006. UI/UX 레이아웃과 토스트 개선

### 작업 요약

저장 상태 표시로 화면 요소 위치가 흔들리는 레이아웃 이동(CLS) 문제를 실제 화면에서 확인해 레이아웃을 안정화하고, 커스텀 확장자 삭제 버튼 디자인을 단순화했다. 이어서 확장자 정책 영역과 파일 업로드 영역을 데스크톱에서 좌우 2단으로 분리하고, 저장 결과 안내를 본문 인라인 문구에서 화면 하단 토스트로 전환했다. 마지막으로 단일 토스트가 최신 알림으로 교체되던 방식을, 알림별 독립 토스트가 최대 3개까지 함께 쌓이는 방식으로 개편했다. 정책 변경 내용은 `PLANNING.md` 5.5절에 반영했다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 레이아웃 이동(CLS) 방지와 화면 분리 요청

```text
현재 화면에서 치명적인 UI/UX 문제가 확인됐어. [...]

핵심 문제는 저장 상태 메시지가 나타났다 사라질 때 주변 요소의 위치가 계속 바뀐다는 점이야. [...] 사용자가 여러 확장자를 연속으로 변경할 때 화면이 반복해서 흔들리므로 사용성이 떨어지고 CLS도 나빠질 가능성이 높아.

1. 저장 상태 때문에 레이아웃이 움직이지 않도록 수정 [...]
2. 커스텀 확장자 삭제 버튼 디자인 단순화 [...]
3. 확장자 정책과 파일 업로드 영역을 명확하게 분리 [...]
```

**요청 의도**

> 저장 상태 문구가 나타나고 사라질 때 체크 칩과 하단 콘텐츠 위치가 흔들려 CLS가 나빠지는 문제를 확인해, 레이아웃 안정화와 화면을 정책/업로드 두 섹션으로 분리하도록 요청했습니다.

#### 2. 좌우 배치와 토스트 전환 요청

```text
화면 레이아웃과 저장 알림 방식을 아래와 같이 수정해줘.

1. 확장자 정책 영역과 파일 업로드 영역을 데스크톱 화면에서 각각 왼쪽과 오른쪽에 배치해줘.
[...]
3. "cmd" 설정이 저장되었습니다. 같은 저장 결과 문구는 본문 안에 출력하지 말아줘.
4. 저장 결과는 본문 레이아웃과 분리된 일반적인 토스트 형태로 표시해줘.
[...]
7. 토스트는 아래에서 위로 자연스럽게 나타났다가 잠시 유지된 후 다시 아래로 사라지도록 애니메이션을 적용해줘.
```

**재요청 이유**

> 레이아웃 안정화 이후에도 정책 영역과 업로드 영역이 세로로만 나열돼 있어, 데스크톱에서는 좌우로 배치하고 저장 결과 문구도 본문이 아닌 토스트로 분리해 레이아웃에 영향을 주지 않도록 다시 요청했습니다.

#### 3. 독립 다중 토스트 전환 요청

```text
토스트가 지금보다 더 잘 보이도록 시각적 구분을 강화해줘. [...]

토스트 처리 방식도 수정해줘.

현재 A 토스트가 표시된 상태에서 B 알림이 들어오면 A의 텍스트를 B로 교체하지 말아줘.

각 알림은 고유한 토스트 항목으로 생성하고 독립적으로 동작하게 해줘. [...]
```

**재요청 이유**

> 기존에는 새 알림이 이전 토스트의 텍스트만 교체하는 방식이었는데, 여러 작업 결과를 사용자가 놓치지 않고 각각 확인할 수 있도록 알림별 독립 토스트가 함께 쌓이는 방식으로 바꾸도록 요청했습니다.

</details>

### AI 활용 과정

- 사용자가 지적한 레이아웃 이동 문제를 실제 화면에서 재현하고 원인을 확인함
- 변경된 UI 정책을 `PLANNING.md`에 반영하고 `document-review`로 정합성을 검토함

### 최종 반영

- 저장 상태 표시로 인한 레이아웃 이동 제거, 커스텀 확장자 삭제 버튼 디자인 단순화
- 화면을 확장자 정책 섹션과 파일 업로드 섹션으로 분리
- 데스크톱 좌우 2단 레이아웃과 하단 슬라이드 토스트로 개편
- 토스트를 알림별 독립 항목으로 최대 3개까지 동시 표시하도록 개편
- `PLANNING.md` 5.5절에 변경된 토스트 정책 반영

### 나의 판단과 회고

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

이번 작업에서는 기존 화면의 레이아웃과 알림 방식을 직접 검토한 뒤 다음과 같이 수정했습니다.

- 저장 상태 표시로 인한 레이아웃 이동 방지
- 커스텀 확장자 삭제 버튼 디자인 단순화
- 확장자 정책과 파일 업로드 영역의 좌우 분리
- 본문 저장 문구를 하단 토스트로 전환
- 연속 알림을 독립 토스트로 표시

> 저장 상태 문구가 나타나고 사라질 때 체크 칩과 하단 콘텐츠 위치가 흔들려 사용성이 떨어지고 CLS도 나빠진다고 판단해 레이아웃을 고정했습니다. 삭제 버튼도 별도 버튼 박스처럼 보이지 않게 단순화하는 것이 화면의 시각적 복잡도를 줄인다고 생각했습니다. 정책 설정과 업로드 실행은 목적이 다른 영역이므로 데스크톱에서는 좌우로 나눠 구분하는 것이 명확하다고 판단했습니다. 저장 결과 문구가 본문 안에 있으면 나타나고 사라질 때마다 레이아웃에 영향을 주므로 본문과 분리된 토스트로 옮겼습니다. 여러 작업 결과를 사용자가 놓치지 않고 각각 확인할 수 있어야 한다고 판단해 알림을 독립적으로 표시하도록 했습니다.

---

## 007. REQUIREMENTS.md 4절 검증 체크리스트 감사와 남은 결함 수정

### 작업 요약

`REQUIREMENTS.md` 4절 검증 체크리스트를 코드와 테스트 기준으로 감사해 만족한 항목을 체크했다. 감사 과정에서 실제 결함이 있는 두 항목을 발견해 미체크로 남겼고, 이어서 두 결함을 모두 수정했다. 첫째, 파일 업로드의 파일명 255바이트 초과 클라이언트 사전 검증에서 오류 문구만 표시되고 포커스가 이동하지 않는 문제를 `FileUploadSection`에서 수정했다. 둘째, 최근 도입한 알림별 독립 토스트 구조(최대 3개까지 쌓는 방식)가 "동일한 오류가 중복으로 쌓이지 않습니다" 요구사항과 충돌하는 문제를, 요구사항 문구는 그대로 두고 구현에 종류(성공/실패)와 메시지 조합 기준의 중복 억제 규칙을 추가해 해결했다. 두 수정 모두 테스트와 헤드리스 브라우저 실사용 검증을 거친 뒤 체크리스트 두 항목을 체크했다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 체크리스트 감사 요청

```text
REQUIREMENTS.md 에 요구사항 만족하면 check 해줘
```

**요청 의도**

> 코드와 테스트를 기준으로 4절 검증 체크리스트의 실제 충족 여부를 확인해 달라는 요청이었습니다.

#### 2. 결함 수정 방향과 제약 조건

```text
[...] REQUIREMENTS.md 문구는 수정하지 말고, 포커스 이동과 토스트 중복 문제를 기존 요구사항에 맞게 구현에서 수정해줘. [...]
```

**재요청 이유**

> 감사 과정에서 발견된 두 결함을 해결하면서, 최근 변경한 토스트 방식에 맞춰 요구사항을 바꾸기보다 기존 요구사항을 유지하고 실제 동작을 이에 맞게 수정하도록 요청했습니다.

</details>

### AI 활용 과정

- Explore 서브에이전트로 검증 체크리스트 약 160개 항목을 감사해, 실제 결함이 있는 두 항목만 미체크로 남김
- 헤드리스 브라우저로 두 결함이 실제로 해결됐는지 확인함
- `document-review`로 체크 상태 변경이 문서 전체와 충돌하지 않는지 검토함

### 최종 반영

- 파일명 검증 실패 시 오류 영역으로 포커스가 이동하도록 보완
- 동일한 종류와 내용의 알림이 중복으로 쌓이지 않도록 토스트 표시 방식 보완
- 두 수정 사항에 대한 테스트 보강
- `REQUIREMENTS.md` 검증 체크리스트 두 항목 완료 처리
- `PLANNING.md`에 토스트 중복 방지 규칙 반영
- `WORKFLOW.md` 검증 단계 상태를 완료로 갱신

### 나의 판단과 회고

#### AI의 제안 중 그대로 반영한 내용은 무엇인가?

- 코드와 테스트를 기준으로 실제 충족된 체크리스트만 완료 처리
- 결함이 확인된 항목은 미체크로 남기고 수정 후 다시 검증

> 요구사항 문서에 적힌 항목을 그대로 체크하기보다, 실제 코드와 테스트 근거를 확인한 뒤 완료 여부를 판단하는 방식이 신뢰할 수 있다고 생각했습니다. 구현되지 않은 항목을 임의로 완료 처리하면 검증 체크리스트의 의미가 없어진다고 판단했습니다.

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

- 새 토스트 정책에 맞춰 요구사항 문구를 바꾸는 대신 구현을 기존 요구사항에 맞게 조정

> 다중 토스트 구조를 유지하기 위해 기존 요구사항 문구를 변경하는 방법도 있었지만, 이미 확정된 요구사항을 구현 편의에 맞춰 바꾸는 것은 적절하지 않다고 판단했습니다.

---

## 008. Cursor Agent 도입과 Claude Code skill 공유 세팅

### 작업 요약

Claude Code 5시간 토큰 한도가 잦게 소진되어, 검증과 배포 등 남은 과제를 Cursor Agent로 이어가기 위해 환경을 구성했다. `.cursor/` 기본 세팅(skill 공유, rules, Superpowers, Context7)에 이어, rule만으로 skill invoke가 보장되지 않아 commands, hooks, mandatory rule 3층 구조를 추가했고, Superpowers brainstorming 등 빠진 routing까지 slash command와 keyword hook으로 보완했다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 프로젝트 분석과 Cursor 동등 환경 방법론

```
이 프로젝트를 분석해줘. Claude Code 기반으로 구성되어 있는데, Cursor에서 동일한 skill 환경으로 동작하게 하려면 어떻게 해야 하는지 방법을 알려줘. 지금 바로 세팅해달라는 게 아니라, 가능성과 방법만 정리해줘.
```

**요청 의도**

> Claude Code로 구성된 프로젝트의 AI 워크플로를 파악하고, Cursor에서 동일한 skill 환경을 구성할 수 있는지와 방법을 먼저 검토하기 위해 요청했습니다.

#### 2. Cursor 세팅 실행

```
`.cursor` 기반으로 Claude Code와 동일하게 동작하도록 세팅해줘.
```

**요청 의도**

> Claude Code 5시간 토큰 한도가 잦게 소진되어 남은 검증과 배포 작업을 Cursor에서 이어가기 위해, Claude Code와 동일하게 동작하는 `.cursor` 환경 구성을 요청했습니다.

#### 3. skill invoke 인프라 구성

```
Claude code 와 똑같이 동작하도록 인프라를 구성해줘.
```

**요청 의도**

> 단순 지침과 skill 문서만 복제하는 것만으로는 Claude Code plugin 수준의 skill invoke가 되지 않았습니다. 따라서 command와 hooks를 포함한 Cursor 인프라를 Claude Code와 최대한 동일하게 맞추기 위해 요청했습니다.

</details>

### AI 활용 과정

- Claude Code 프로젝트 구조를 분석하고 Cursor 동등 환경 구성 방법과 체크리스트를 정리함
- `.cursor/`에 Superpowers, Context7, skill, rules를 구성하고 `.claude/skills/`와 동일 skill을 공유하도록 세팅함
- rule/skill catalog만으로 invoke가 보장되지 않음을 확인하고, commands + hooks + mandatory rule 3층 invoke 구조를 추가함
- Superpowers brainstorming 등 빠진 routing을 분석해 slash command 10개와 keyword hook 전체 mapping을 보완함
- `AI_WORKFLOW.md`, `WORKFLOW.md`, `AGENTS.md`, `using-project-skills`를 routing spec에 맞게 갱신함

### 최종 반영

- `.cursor/` 기본 구성 (settings, mcp, skills, rules) 및 `AGENTS.md` 추가
- Claude Code skill(`document-review`, `prompt-log`) 공유, `commit`/`context7` skill 추가
- skill invoke 인프라: `.cursor/commands/` 10개, `.cursor/hooks.json`, hook script 3개, `using-project-skills`, `00-skill-invocation-required.mdc`
- `route-skill-on-prompt.py`에 brainstorming, writing-plans, SDD, TDD, debug, verify, context7 keyword routing 추가
- `AI_WORKFLOW.md`, `WORKFLOW.md`, `.env.example` 갱신

### 나의 판단과 회고

#### AI의 제안 중 그대로 반영한 내용은 무엇인가?

- `.claude/skills/`의 구성을 `.cursor/skills/`에 동일하게 복사
- `CLAUDE.md`의 규칙을 `AGENTS.md`에 이식
- Superpowers, Context7, `commit`와 `context7` skill 및 기본 rules 구성

> Claude Code의 5시간 토큰 한도에 자주 도달해, 남은 작업은 Cursor에서 이어가기로 했습니다. 사용하는 Agent가 달라져도 `AI_WORKFLOW.md`의 절차가 끊기지 않도록 skill과 규칙을 동일하게 공유하는 편이 적절하다고 판단했습니다. 이에 AI가 제안한 skill 복사, `AGENTS.md` 규칙 이식, Superpowers와 Context7의 기본 구성을 그대로 반영했습니다.

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

- command, hook, mandatory rule로 구성된 3단계 skill 호출 구조 추가(prompt #3의 skill 호출 환경 구성)
- brainstorming, writing-plans, execute-plan, debug, verify, context7용 slash command와 keyword hook 보완

> 초기 구성만으로는 Agent가 rule과 skill catalog를 직접 해석해 적절한 skill을 선택해야 했기 때문에, skill 호출이 항상 일관되게 이루어지기는 어렵다고 판단했습니다. 이를 Claude Code plugin과 유사한 수준으로 보완하기 위해 slash command, hook, mandatory rule을 결합한 3단계 구조를 추가했습니다. 이후 brainstorming 등 일부 Superpowers routing이 빠져 있는 것도 확인해 관련 keyword hook과 slash command를 함께 보완했습니다.

---

## 009. 커스텀 확장자 UI 분리와 확장자 정책 레이아웃 정리

### 작업 요약

커스텀 확장자 기능의 상태 관리, 입력, 목록 표시 책임을 분리하고 기존 Tailwind 기반 화면에 통합했다. 이후 확장자 정책과 파일 업로드 영역을 데스크톱에서 좌우로 배치하고, 업로드 최대 크기와 확장자 설정을 하나의 정책 영역으로 구성했다.

또한 커스텀 확장자 삭제 버튼 표현을 단순화하고, 연속된 마침표 입력을 별도의 오류 사유로 구분했다. 변경된 화면 구성과 판단 근거는 관련 문서에 반영했다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 커스텀 확장자 컴포넌트 구조 이식

```
ui-ux-improvements 워크트리에 구현된 커스텀 확장자 컴포넌트 분리 구조와 테스트를 검토하고, Tailwind가 적용된 file-upload-implementation 워크트리에 동일한 동작이 유지되도록 이식해줘.

기존 검증 규칙과 스타일을 유지하면서 상태 관리, 입력 영역, 목록 표시 책임을 분리하고 관련 테스트도 함께 반영해줘.
```

**요청 의도**

> 기존 커스텀 확장자 기능을 하나의 컴포넌트에서 관리하지 않고 상태 관리, 입력, 목록 표시 책임을 분리해 유지보수성을 높이고, 기존 동작이 유지되도록 이식하기 위해 요청했습니다.

#### 2. 확장자 정책과 파일 업로드 화면 구조 정리

```
데스크톱 화면에서 확장자 정책 영역과 파일 업로드 영역을 동일한 수준의 독립된 섹션으로 구성해줘.

왼쪽에는 확장자 정책, 오른쪽에는 파일 업로드를 배치하고, 확장자 정책 영역에는 업로드 최대 크기 설정과 고정 확장자, 커스텀 확장자 설정을 포함해줘.

각 섹션 제목은 카드 바깥에 배치하고, 확장자 설정 제목과 설명은 해당 카드 내부에 배치해 정보 계층이 명확하게 보이도록 구성해줘.
```

**요청 의도**

> 확장자 정책과 파일 업로드는 서로 다른 역할을 가지므로 화면에서도 독립적으로 구분하고, 업로드 허용 여부를 결정하는 설정은 하나의 정책 영역으로 묶기 위해 요청했습니다.

#### 3. 연속된 마침표 오류 처리 보완

```
커스텀 확장자 입력에서 연속된 마침표가 포함된 경우를 일반 형식 오류와 구분해서 처리해줘.

정규화 단계와 서버 API가 동일한 오류 사유를 사용하고, 화면 검증과 서버 최종 검증이 같은 기준을 사용하도록 테스트도 보완해줘.
```

**요청 의도**

> 연속된 마침표 입력이 일반 형식 오류와 동일하게 처리되는 문제를 수정하고, 클라이언트와 서버의 검증 기준을 일치시키기 위해 요청했습니다.

</details>

### AI 활용 과정

- ui-ux-improvements 워크트리의 컴포넌트 분리 구조와 테스트를 검토하고, 기존 Tailwind 구현과 비교해 이식 시 유지해야 할 동작 범위를 확인함
- 사용자가 제시한 배치 방향에 따라 섹션 제목과 카드 경계의 위치를 두 차례 재검토함
- glob 패턴 도입 시 늘어나는 정책 정의 범위(패턴 문법, 위험 패턴 제한, 고정 확장자와의 관계)를 기존 리터럴 정책과 비교 검토함
- 클라이언트 정규화와 서버 API의 오류 사유가 일치하는지 점검하고, 화면 검증과 서버 최종 검증 기준을 맞추는 방법을 검토함

### 최종 반영

- 확장자 정책과 파일 업로드 영역의 데스크톱 좌우 배치 적용
- 확장자 정책 영역에 업로드 최대 크기와 확장자 설정 통합
- 커스텀 확장자 삭제 버튼 표현 단순화
- `PLANNING.md`에 변경된 화면 구조와 컴포넌트 분리 내용 반영
- `CONSIDERATIONS.md`에 접근성, 반응형과 glob 패턴 제외 근거 반영
- 연속된 마침표 입력에 `CONSECUTIVE_DOTS` 오류 사유와 전용 안내 문구 적용

### 나의 판단과 회고

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

이번 작업에서는 기존 화면 구성을 검토한 뒤 다음 내용을 수정했습니다.

- 확장자 정책과 파일 업로드 영역을 좌우 독립 영역으로 분리
- 업로드 최대 크기를 확장자 정책 영역에 포함
- 확장자 설정 제목과 설명을 카드 내부로 배치
- 연속된 마침표 입력을 별도 오류 사유로 처리

> 커스텀 확장자가 추가될수록 목록 영역이 길어지면서 파일 업로드 영역이 아래로 밀리는 문제가 발생했습니다. 이로 인해 사용자가 설정을 변경할 때마다 화면 위치가 변하고 CLS 저하 가능성이 있다고 판단해, 확장자 정책과 파일 업로드 영역을 좌우로 분리했습니다. 또한 업로드 최대 크기와 확장자 설정은 모두 업로드 정책을 결정하는 요소이므로 하나의 정책 영역으로 구성했습니다. 연속된 마침표 입력은 사용자가 수정해야 하는 원인이 명확하므로 일반 형식 오류와 구분해 별도의 오류 사유로 처리했습니다.

#### AI의 제안 중 제외한 내용은 무엇인가?

이번 작업에서는 glob 패턴 도입을 검토했지만 적용하지 않았습니다.

> 더 나은 관리 편의성을 위해 glob 패턴 도입을 검토했습니다. 하지만 glob을 적용하면 등록값이 단순한 확장자 이름이 아니라 파일명 매칭 규칙으로 변경되어, 패턴 문법, 위험 패턴 제한, 고정 확장자와의 관계, 입력 검증 등 추가 정책 정의가 필요합니다. 현재 서비스는 특정 확장자를 차단하는 목적이므로 파일명 규칙 엔진으로 범위를 확장하는 것은 적절하지 않다고 판단해 glob 패턴은 도입하지 않았습니다.

---

## 010. 커스텀 확장자 일괄 입력, extignore, 초기화와 업로드 목록

### 작업 요약

커스텀 확장자에 단일/일괄 모드, `extignore.txt` 가져오기/내보내기, 정책 초기화를 추가했다. 이어서 과제 검증을 위해 업로드 파일 목록을 공개하고, 보호 시드만 삭제를 막아 데모에서 샘플을 다시 받을 수 있게 했다. 관련 정책과 UI 결정은 `REQUIREMENTS.md`, `PLANNING.md`, `CONSIDERATIONS.md`에 반영했다.

### 프롬프트 기록

<details>
<summary>프롬프트 보기</summary>

#### 1. 일괄 입력과 extignore 범위

```
기존 커스텀 확장자 입력 기능을 다음 방향으로 개정해줘.

glob 패턴은 지원하지 않는 것으로 확정했어. 이번 작업은 단일 확장자 입력, 텍스트 기반 일괄 입력,
가져오기와 내보내기를 지원하는 방향으로 요구사항과 설계를 정리하는 것이 목표야.

- 커스텀 영역에 단일/일괄 모드를 두고 세그먼트로 전환한다
- 모드 전환 시 입력값은 확인 없이 즉시 초기화한다
- 일괄은 쉼표로 구분하고, 각 항목마다 최대 20자를 적용한다
- 형식 오류가 하나라도 있으면 부분 저장 없이 전체 실패로 처리한다
- 차단 목록을 파일로 가져오고 내보낼 수 있게 한다
```

**요청 의도**

> 단건 입력의 `n/20` 정책은 유지한 채, 여러 확장자를 한 번에 넣고 파일로 옮길 수 있는 경로를 만들기 위해 요청했습니다.

#### 2. 초기화 범위와 명칭

```
정책을 되돌리는 버튼은 "전체 삭제"보다 "초기화"로 해줘.

동작 범위는 다음으로 정해줘.
- 등록된 커스텀 확장자는 전부 삭제
- 활성 상태인 고정 확장자는 전부 비활성화
- 업로드 최대 크기 설정은 건드리지 않음
```

**재요청 이유**

> 커스텀만 지우는 삭제가 아니라, 고정 확장자 체크까지 기본 상태에 가깝게 되돌리는 동작이 맞다고 판단해 명칭과 범위를 바꿨습니다.

#### 3. 가져오기 오류 미표시

```
일괄 입력과 extignore 가져오기는 형식 오류가 하나라도 있으면 전체 반영하지 않아야 해.

화면에는 아래처럼 오류 메시지가 표시되어야 하는데 현재 보이지 않아.

올바르지 않은 항목이 있습니다: sqlite-wal, sqlite-shm

가져오기 실패 시 사용자에게 오류 피드백이 보이도록 수정해줘.
```

**재요청 이유**

> 전체 실패로 설계했는데 화면에 원인이 안 보이면 검증과 사용이 불가능하다고 판단했습니다.

#### 4. 업로드 목록 공개

```
파일 업로드 목록이 필요할 것 같아.
현재 REQUIREMENTS와 다르더라도, 추가하게 된 이유를 정리해서 이번 구현 범위에 포함해줘.

다른 사용자가 업로드한 파일도 다운로드할 수 있어야 해.
고정으로 제공하는 파일만 보호하고, 나머지 업로드 파일은 삭제와 관리가 가능하도록 구현해줘.
```

**요청 의도**

> 업로드가 실제로 저장됐는지 확인하고 샘플을 받아 가져오기를 시험하려면 목록이 필요하다고 판단했습니다. 인증을 넣지 않는 대신 공개 데모로 범위를 바꾸고, 시드만 삭제로부터 보호하기로 했습니다.

</details>

### AI 활용 과정

- glob은 제외로 확정하고, 일괄은 리터럴·항목별 20자·형식 오류 시 전체 실패로 설계함
- 파일명은 숨김 `.extignore` 대신 `extignore.txt`로 확정함(선택 UX, gitignore형 오해 방지)
- 초기화는 커스텀 전체 삭제 + 고정 전체 비활성, 업로드 최대 크기는 제외, 확인 대화 후 실행
- 가져오기 오류가 일괄 영역에만 있어 단일 모드에서 안 보이던 원인을 찾아 확장자 카드 공통 표시로 고침
- 목록 비공개 정책을 공개 공유 데모로 개정하고, 보호 시드 4개와 목록/다운로드/삭제 API·UI를 추가함

### 최종 반영

- 단일/일괄 모드, 일괄 등록 API, `extignore.txt` 가져오기/내보내기, 정책 초기화
- 가져오기/내보내기/초기화 버튼을 모드 토글 아래 오른쪽으로 배치하고 라벨을 축소
- 가져오기·일괄 형식 오류와 한도 초과를 모드와 무관하게 인라인 표시
- 업로드 목록 공개, 보호 시드, 다운로드/삭제(`is_protected`는 삭제만 불가)
- `REQUIREMENTS.md`, `PLANNING.md`, `CONSIDERATIONS.md` 관련 절 정합

### 나의 판단과 회고

#### AI의 제안 중 그대로 반영한 내용은 무엇인가?

- 일괄/가져오기 형식 오류 시 전체 실패, 기존 커스텀 중복은 일괄에서만 조용히 제외
- 초기화 범위(업로드 크기 제외)와 확인 절차
- 업로드 목록 공개와 보호 시드 구분

> 일괄 입력과 가져오기는 일부 항목만 저장되면 사용자가 현재 정책 상태를 예측하기 어렵다고 판단해 전체 실패 방식으로 유지했습니다. 또한 초기화는 되돌릴 수 있는 범위가 명확해야 하므로 커스텀 확장자와 고정 확장자 상태만 변경하고 업로드 최대 크기는 유지했습니다. 업로드 목록은 데모 환경에서 기능 검증을 위해 필요하다고 판단했고, 테스트용 시드는 보호 상태로 관리하는 방식을 적용했습니다.

#### AI의 제안을 수정해서 반영한 내용은 무엇인가?

- 가져오기/내보내기 기본 파일명을 `.extignore`에서 `extignore.txt`로 변경
- 가져오기 오류 표시 위치를 모드 공통 영역으로 이동

> .extignore 형식은 관리 측면에서는 적합하지만 브라우저 파일 선택 과정에서 숨김 파일로 취급될 가능성이 있고, gitignore 계열 파일과 혼동될 수 있다고 판단했습니다. 따라서 일반 사용자가 쉽게 생성하고 업로드할 수 있도록 extignore.txt 형식으로 변경했습니다. 또한 가져오기 오류는 특정 입력 방식에 종속되면 사용자가 찾기 어렵기 때문에 확장자 정책 영역에서 공통으로 표시하도록 변경했습니다.