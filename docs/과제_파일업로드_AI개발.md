# 과제. 파일 업로드 처리 & AI 활용 개발 과제

> 기존 "파일 확장자 차단 페이지" 과제를 개편한 버전입니다.
> 이번 과제는 **화면 구현 그 자체보다**, ① 파일 업로드라는 기능을 다룰 때 **기획/개발 관점에서 무엇을 고려하는가**, 그리고 ② 그 과정에서 **AI(코딩 에이전트/LLM)를 얼마나 잘 활용하는가** 를 봅니다.

---

## 1. 배경 & 목적

어떤 파일들은 첨부/업로드 시 보안에 문제가 될 수 있습니다. 특히 `exe`, `sh` 등의 실행 파일이 서버에 올라가 실행될 경우 위험이 있어, 파일 확장자 차단을 포함한 **안전한 파일 업로드 처리**가 필요합니다.

이번 과제의 목적은 두 가지입니다.

1. **파일 업로드 기능을 실제로 동작하는 형태로 구현**하고 배포한다.
2. 구현보다 더 중요한 것 — **어떤 점들을 고려했는지(기획/보안/예외/운영)**, 그리고 **그 판단·구현 과정에서 AI를 어떻게 사용했는지**를 문서로 남긴다.

> 정답이 정해진 과제가 아닙니다. "무엇을 고려했고, 왜 그렇게 판단했는가"의 **깊이와 근거**를 봅니다.

---

## 2. 과제 개요

파일 업로드 시 **확장자 기반 차단 정책**을 관리하고, 실제 파일 업로드 시 그 정책이 적용되는 기능을 만드세요.

### 최소 구현 범위 (필수)

**A. 확장자 차단 정책 관리 화면**

- **고정 확장자**: 차단을 자주 하는 확장자 리스트 (`bat`, `cmd`, `com`, `cpl`, `exe`, `scr`, `js`)
  - default는 **unCheck** 상태
  - check/uncheck 시 **DB에 저장** → 새로고침 시 유지
  - (고정 확장자는 아래 커스텀 확장자 영역에 표시되지 않음)
- **커스텀 확장자**
  - 입력 최대 길이 **20자**
  - "추가" 클릭 시 DB 저장 후 목록 영역에 표시
  - 최대 **200개**까지 추가 가능
  - 각 항목 옆 `X` 클릭 시 DB에서 삭제
  - 중복 추가 방지 (예: `sh` 추가 후 다시 `sh` 추가 시 처리)

**B. 실제 파일 업로드 처리**

- 위에서 설정한 차단 정책이 **실제 업로드에 적용**되어야 함
- 차단 대상 파일 업로드 시 명확한 사유와 함께 거부
- 정상 파일은 업로드 성공 처리 (저장 위치/방식은 자유)

> A만 있고 B가 없으면 "정책은 있으나 실제로 막지 못하는" 화면일 뿐입니다. **정책이 실제 업로드에서 강제되는 것**까지가 최소 범위입니다.

---

## 3. 기획/개발 고려사항 (핵심 평가 영역)

아래는 **"이런 것도 고려했나요?"** 를 묻는 항목입니다. 전부 구현할 필요는 없습니다.
하지만 **구현했든, 의도적으로 제외했든, 각 항목에 대한 판단과 근거를 문서에 남기세요.** ("고려 안 함"도 근거가 있으면 됩니다.)

### 3-1. 검증/보안 관점
- 확장자만 믿어도 되는가? (실제 파일 내용/시그니처(매직 넘버)와의 불일치 — 예: `report.jpg` 인데 실제로는 실행 파일)
- 대소문자 / 이중 확장자 (`file.exe.txt`, `file.PDF`, `.tar.gz`)
- 확장자 없는 파일, 점(.)으로 시작하는 파일(`.env`), 매우 긴 파일명
- 확장자 입력값 검증 (특수문자·공백·유니코드·`.` 포함 여부 정규화)
- 서버 사이드 검증의 필요성 (클라이언트 검증만으로는 우회 가능)
- 업로드 파일 크기 제한 / 파일 개수 제한
- 저장 시 원본 파일명 사용 위험 (경로 조작, 실행 가능 위치 저장) 및 대응
- MIME 타입 스푸핑

### 3-2. 정책/데이터 관점
- 고정 확장자와 커스텀 확장자가 겹칠 때 (예: 커스텀에 `exe` 입력)
- 정책 변경 이력/감사(누가 언제 무엇을 바꿨는지)의 필요 여부
- 200개 / 20자 등 제한의 근거와 초과 시 UX
- 대량(수백 개) 확장자 조회/저장 시 성능·인덱스 고려

### 3-3. UX/예외 관점
- 차단 시 사용자에게 보여줄 메시지(무엇이, 왜 막혔는지)
- 로딩/에러/네트워크 실패 상태 처리
- 저장 실패 시 화면 상태와 DB 상태의 일관성
- 접근성/반응형(선택)

### 3-4. 운영 관점
- 새로고침/동시 편집 시 정합성
- 로그/모니터링 관점에서 무엇을 남길 것인가
- 향후 확장(예: 사용자별 정책, 화이트리스트 방식 전환) 고려 여부

> ⚠️ 위 목록은 예시입니다. **여기 없는 고려사항을 스스로 찾아내는 것**을 특히 높게 봅니다.

---

## 4. AI 활용 & 개발 기록 (필수 제출)

이번 과제의 절반은 **"AI를 어떻게 썼는가"** 입니다. 다음을 제출하세요.

### 4-1. 프롬프트/입력 기록
- 개발하면서 AI(코딩 에이전트, LLM 등)에 **입력한 텍스트(프롬프트)를 시간 순으로 기록**
- 그대로 붙여넣기보다는, **어떤 의도로 그렇게 물었는지 / 결과가 별로였을 때 어떻게 다시 물었는지**가 드러나면 좋습니다.
- 형식 자유 (예: `PROMPT_LOG.md`, 대화 export, 스크린샷 등)

### 4-2. 스킬 / 플러그인 사용 표시
- 사용한 **스킬, 플러그인, 확장, MCP, 에이전트, 라이브러리/도구**를 명시
- 각각 **어디에(어떤 작업에) 왜 썼는지** 한 줄씩 표기
  - 예) `xxx 스킬 — 확장자 검증 로직 테스트 코드 생성에 사용`

### 4-3. 판단 근거 회고 (짧게)
- AI가 준 결과 중 **그대로 쓴 것 / 고쳐 쓴 것 / 버린 것**과 그 이유
- AI가 놓쳤거나 틀렸는데 본인이 잡아낸 부분이 있으면 강조

> 목적: 결과물이 아니라 **사고 과정과 AI 활용 역량**을 보기 위함입니다. 화려한 프롬프트보다 **문제를 쪼개고 검증하는 방식**을 봅니다.

---

## 5. 제출 항목

| 구분 | 항목 | 비고 |
|------|------|------|
| 필수 | **GitHub 주소** | table schema 포함, 실행 방법(README) 포함 |
| 필수 | **배포된 사이트 URL** | 면접 당일 실제 호출 가능한 주소 제공 (누구나 접속 가능) |
| 필수 | **고려사항 문서** (`CONSIDERATIONS.md`) | 3번 항목에 대한 판단/근거 |
| 필수 | **AI 활용 기록** (`PROMPT_LOG.md` 등) | 4번 항목 — 프롬프트·스킬/플러그인·회고 |

- **table schema**: 확장자 정책을 저장하는 테이블 정의(컬럼/타입/제약/인덱스)를 반드시 포함
- **배포**: 무료 호스팅(예: Vercel, Render, Railway, Cloudtype 등) 무엇이든 가능. **면접 당일 접속 가능**해야 합니다.

---

## 6. 평가 기준 (가중치는 참고용)

| 항목 | 내용 | 비중 |
|------|------|------|
| 고려사항의 깊이 | 파일 업로드 관련 기획/보안/예외를 얼마나 넓고 깊게 고려했는가 | ★★★★ |
| AI 활용 역량 | AI를 문제 분해·검증·반복 개선에 잘 썼는가, 결과를 비판적으로 취사선택했는가 | ★★★★ |
| 동작/완성도 | 필수 기능이 실제로 동작하고 배포되어 있는가 | ★★★ |
| 코드 품질 | 가독성, 구조, 서버/클라이언트 검증 분리, 테스트 | ★★ |
| 문서화 | 스키마·README·기록의 명확성 | ★★ |

> 규모가 크지 않아도 됩니다. **"왜 이렇게 했는지"가 설명되는 작은 결과물** > "설명 없는 큰 결과물".

---

## 7. 참고 / FAQ

- **기술 스택 자유** — 프론트/백엔드/DB 무엇이든 무방
- **범위가 부담되면** 필수 범위(2번)만 완성하고, 3번은 "구현은 못 했지만 이렇게 고려했다"로 문서화해도 됩니다.
- **가장 피해야 할 것**: AI가 준 코드를 검증 없이 그대로 붙여 넣고, 왜 그런지 설명 못 하는 상태
- **가장 좋게 보는 것**: 요건에 없는 위험/예외를 스스로 발견 → AI와 함께 검증 → 근거를 남긴 흔적

---

### (원본 과제 화면 참고 이미지 설명)

```
◎ 파일 확장자 차단
파일확장자에 따라 특정 형식의 파일을 첨부하거나 전송하지 못하도록 제한

① 고정 확장자   □ bat  □ cmd  □ com  □ cpl  □ exe  □ scr  □ js
② 커스텀 확장자  [ 확장자 입력            ] (+추가)
③               3/200
                [ sh X ] [ ju X ] [ ch X ]
```
